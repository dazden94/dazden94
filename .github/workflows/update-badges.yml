name: Update badges

on: workflow_dispatch
  #schedule:
    # Runs at 0am UTC every day
  #  - cron: "*/5 * * * *"
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: pemtajo/badge-readme@mainR
        with:
          CREDLY_USER: nedzad-winter.774b6a80
